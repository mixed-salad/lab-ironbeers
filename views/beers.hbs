<div>
    <ul class="beersList">
{{#each beersList}}
<li>
    <img src="{{this.image_url}}" alt="">
    <p>{{this.name}}</p>
    <p>{{this.description}}</p>
    <p>{{this.tagline}}</p>
    </li>
{{/each}}
    </ul>
</div>